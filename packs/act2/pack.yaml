# Pack manifest (metadata authored; `files:` regenerated by generatePackManifests / shipPack).
# This pack is NOT bundled: it is the DLC sample. `./gradlew shipPack -Ppack=act2` zips it; drop the
# zip into the content root (~/.prpg/content/) and the keeper's "road" branch opens.
schemaVersion: 1
id: act2
kind: act
version: 1
bundled: false
requiresBaseline: true
baselineVersion: ">=1"
provides:
  - id: act2
    title: The Road
    order: 2
    entryMap: road
    entrySpawn: start
flags:
  - act2.met_traveller
requiresFlags:
  - act1.act_complete     # read by this pack's Ink; declared by act1
files:
  - actors/traveller.yaml
  - flags.yaml
  - maps/road.tmx
  - narrative/act2.ink.json
  - staging/act2.yaml
